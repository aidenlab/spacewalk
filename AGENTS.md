# Spacewalk — Agent Guide

## Agent skills

### Issue tracker

Issues and PRDs live in GitHub Issues on `aidenlab/spacewalk`, driven via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles, using their default label strings. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` at the root plus `docs/adr/`, alongside the pre-existing `development-notes/` store. See `docs/agents/domain.md`.
